// FormValidator еще один логический блок вашей программы, а значит отдельный класс
// Конструктор FormValidator должен принимать 2 параметра — список полей, которые вы раньше передавали в enableValidation и ссылку на форму, которую вы валидируете
// Все остальные методы, которые относились к enableValidation станут приватными методами класса FormValidator
// У вас должно получиться около 3-5 файлов: 2 файла для каждого из классов + главный файл где запускается/собирается вся логика приложения + в случае необходимости файл utils.js куда вы можете вынести какие-нибудь функции которые вы используете в нескольких файлах + по желанию файл constants.js в котором будут лежать константы вашего приложения
// В основном файле вам надо создать по экземпляру класса на каждую карточку (как на начальные, так и на новые) + по одному экземпляру на каждую форму которую вы хотите валидировать. В случае форм создание экземпляров класса не запускает валидацию автоматически, поэтому надо вызвать еще по крайней мере один публичный метод чтобы она заработала